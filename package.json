{
  "name": "mock-practice",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "open": "node scripts/build.mjs && node scripts/open.mjs",
    "binary": "node scripts/build.mjs && bun build --compile ./scripts/serve.mjs --asset ./dist --outfile ./mock-practice"
  },
  "devDependencies": {
    "esbuild": "^0.25.10"
  }
}
